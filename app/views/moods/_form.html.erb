<%= form_with(model: mood, local: true) do |form| %>
  <% if mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mood.errors.count, "error") %> prohibited this mood from being saved:</h2>

      <ul>
      <% mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :emotion %>
    <%= form.text_field :emotion, id: :mood_emotion %>
  </div>

  <%= form.hidden_field :mood_id %>

  <!-- <div class="field">
    <%= form.label :journal_id %>
    <%= form.number_field :journal_id, id: :mood_journal_id %>
  </div> -->


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
